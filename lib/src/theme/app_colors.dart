import 'package:flutter/material.dart';

sealed class AppColors {
  static const white = Colors.white;
  static const blue = Color.fromRGBO(133, 195, 222, 1);
  static const realBlack = Color.fromRGBO(0, 0, 0, 1);
}